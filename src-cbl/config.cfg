move "/"                           to routing-pattern(1).
move "dashboard"                   to routing-destiny(1).

move "/push"                       to routing-pattern(2).
move "push"                        to routing-destiny(2).

move "/showname/%value"            to routing-pattern(3).
move "showname"                    to routing-destiny(3).

